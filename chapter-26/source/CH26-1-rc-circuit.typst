#set page(fill: rgb("#26233a"))
#set text(fill: rgb("#e0def4"))

= RC Circuit

$tau$ = time constant, amount of time for $q$ to reach $63.2\%$ of $epsilon C$

$display(
q_"max" = epsilon C
\
i_"max" = epsilon / R
)$

$display(
tau = R C
)$

#heading(level: 2, "Charging")
\
$display(
q = epsilon C (1 - e^(-t / tau))
\
i = epsilon / R e ^(-t / tau)
)$

#heading(level: 2, "Discharging")
\
$display(
q = epsilon C e^(-t / tau)
\
i = epsilon / R e^(-t / tau)
)$

